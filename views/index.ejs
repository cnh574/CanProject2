<!DOCTYPE html>
<html lang="en">
  <%- include ('./partials/head.ejs') %>
  <body>
    <h1>Shadez for Dayz!</h1>
    <div class="container">
      <% for (let i = 0; i < shades.length; i++) { %>
      <div class="shades-home-page">
        <h1><%=shades[i].name%></h1>
        <a href="/shades/<%=shades[i].id%>">
          <img src="<%=shades[i].image%>" />
        </a>
      </div>

      <% } %>
    </div>
  </body>
</html>
